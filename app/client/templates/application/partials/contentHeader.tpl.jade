.content-header
  h1 {{_ title}}
    if titleCount
      {{getPublishedCount titleCount}}
    small
      if subtitleCount
        {{getPublishedCount subtitleCount}}
      if subtitle
        {{_ subtitle}}
