Feature: Close Browser

  This is a workaround for the tests
  And it must be the last scenario (by filename in alphabetical order)
  So that the selenium grid session is closed properly

  Scenario: Goodbye
    Then I close the browser
