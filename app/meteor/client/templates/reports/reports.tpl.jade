.content-header
  h1
    | {{_ 'reports.thisDaySingular' }} {{ calendarDay day }}
    small {{ weekOfYear day }}
  +reportActions

.content
  if currentReport
    +with currentReport
      +report
  else
    +box(class='warning' title='ui.notice' body='reports.empty')
