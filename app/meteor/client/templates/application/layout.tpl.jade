if loaded
  #loaded

#locale(class=locale)

+sAlert

#main
  .disable-select
    +AdminLTE(skin='blue' fixed=true sidebarMini=true)
      if currentUser
        +topbar
        +sidebar
        .content-wrapper
          +Template.dynamic template=main
      else
        +lockedLayout
          +login

    +modals
