export const newAppointment = (state = {}, action) => {
  switch (action.type) {
    case 'PATIENT_CHANGE_VALUE':
      return {}
    case 'APPOINTMENT_INSERT_SUCCESS':
      return {}
    default:
      return state
  }
}
