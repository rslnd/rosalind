dependencies:
  cache_directories:
    - ~/.meteor
    - /usr/local/bin

  override:
    - meteor --version || curl https://install.meteor.com | /bin/sh

checkout:
  post:
    - cp ./settings.json.example ./settings.json
